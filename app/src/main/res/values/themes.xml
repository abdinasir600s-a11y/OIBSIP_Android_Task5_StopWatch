<?xml version="1.0" encoding="utf-8"?>
<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Base application theme: Light, NoActionBar, no forced dark -->
    <style name="Theme.StopWatch" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="colorPrimary">@color/primary_blue</item>
        <item name="colorPrimaryDark">@color/primary_blue</item>
        <item name="colorAccent">@color/accent_purple</item>
        <!-- Explicit light window background - prevents black screen -->
        <item name="android:windowBackground">@color/light_background</item>
        <!-- Keep status bar blue and nav bar matching background -->
        <item name="android:statusBarColor">@color/primary_blue</item>
        <item name="android:navigationBarColor">@color/light_background</item>
        <!-- Disable forced dark mode on API 29+ -->
        <item name="android:forceDarkAllowed" tools:targetApi="q">false</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowActionBar">false</item>
    </style>

</resources>
